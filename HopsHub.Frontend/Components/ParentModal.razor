@*@using HopsHub.Shared.DTOs;

<RatingModal @ref="_ratingModal" />
<CreateBeerModal @ref="_createBeerModal" OnBeerCreated="HandleBeerCreated" />

@code {
    private RatingModal _ratingModal;
    private CreateBeerModal _createBeerModal;

    private void OpenCreateBeerModal()
    {
        _createBeerModal.OpenModal();
    }

    private void HandleBeerCreated(SelectBeerDTO createdBeer)
    {
        // Pass the created beer to the rating modal
        _ratingModal.SelectBeer(createdBeer);
    }
}*@
